{
  "name": "zalominiapp-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Zalo Mini App Lucky Wheels Monorepo containing Backend API, Mini App, and Admin Web.",
  "scripts": {
    "test": "npm --prefix backend test && npm --prefix lucky-wheels test -- --run",
    "test:db": "npm --prefix backend run test:db",
    "build": "npm --prefix lucky-wheels run build && npm --prefix admin-web run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:admin": "npm --prefix admin-web run dev",
    "dev:mini": "npm --prefix lucky-wheels run start"
  }
}
