FROM node:22-alpine AS runner

WORKDIR /app

# Install production dependencies
COPY package*.json ./
RUN (npm ci --omit=dev || npm install --omit=dev) && npm cache clean --force

# Copy source code
COPY src/ ./src/

ENV NODE_ENV=production

CMD ["node", "src/delivery-worker.js"]

