APP_ID=
ZMP_TOKEN=

# Backend API. Supabase/ZBS credentials belong to ../backend/.env only.
# `npm run build` creates the web target; `npm run build:miniapp` creates the Zalo Mini App target.
VITE_API_BASE_URL=https://luckyapi.wiai.vn/api/v1
